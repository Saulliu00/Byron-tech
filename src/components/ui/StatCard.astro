---
interface Props {
  value: string;
  label: string;
}

const { value, label } = Astro.props;
---

<div class="text-center">
  <div class="text-4xl font-bold text-white mb-1">{value}</div>
  <div class="text-sm text-primary-200">{label}</div>
</div>
